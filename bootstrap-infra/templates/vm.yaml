---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.vm.name }}
  namespace: {{ .Values.vm.namespace }}
type: Opaque
stringData:
  accessKeyId: {{ .Values.vm.sourceAccessKeyId }}
  secretKey: {{ .Values.vm.sourceSecretAccessKey }}
---
apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: {{ .Values.vm.name }}
  namespace: {{ .Values.vm.namespace }}
spec:
  source:
    s3:
      url: {{ .Values.vm.sourceUrl }}
      secretRef: {{ .Values.vm.name }}
  storage:
    storageClassName: {{ .Values.vm.storageClass }}
    resources:
      requests:
        storage: {{ .Values.vm.size }}
---
apiVersion: cdi.kubevirt.io/v1beta1
kind: DataSource
metadata:
  name: {{ .Values.vm.name }}
  namespace: {{ .Values.vm.namespace }}
spec:
  source:
    pvc:
      name: {{ .Values.vm.name }}
      namespace: {{ .Values.vm.namespace }}
